\ProvidesPackage{style}
\KOMAoptions{paper=a4,fontsize=12pt,twoside,open=any,draft=false}

% PDF 1.6
\pdfminorversion=6
\usepackage{morewrites}
\usepackage{wrapfig}
\usepackage{pbox}

\addtolength{\parskip}{-0.3mm}
\setlength\parindent{0pt}


% ------------------------------------------------------------------------------
% Font related
% ------------------------------------------------------------------------------
% Use 8-bit encoding that has 256 glyphs
\usepackage[T1]{fontenc}

% UTF8 input encoding
\usepackage[utf8]{inputenc}

% Adobe Utopia Fonts
\usepackage{lmodern}

%Font Awesome Font
\usepackage{fontawesome}

% Sans-serif looks more modern
\renewcommand*\familydefault{\sfdefault}

\renewcommand{\arraystretch}{1.3}

% ------------------------------------------------------------------------------


% ------------------------------------------------------------------------------
% General
% ------------------------------------------------------------------------------
\usepackage{import}

\usepackage{lipsum}
\usepackage{amsmath,amsfonts,amsthm}
\usepackage[ngerman]{babel}
\usepackage{verbatim} % comments
\usepackage{xspace}

\usepackage{pdfpages}

%ToDo Notes
\usepackage[color=red]{todonotes}
\let\svtodo\todo\renewcommand\todo[1]{\svtodo[inline]{\textcolor{white}{#1}}}

% Multi-line footnote indent
\deffootnote[1em]{1em}{1em}{\textsuperscript{\thefootnotemark}\,}


\usepackage{etoolbox}
\usepackage{xpatch}
\tracingpatches

\usepackage{xifthen}
\usepackage{xstring}
\usepackage[binary-units=true]{siunitx}
\sisetup{
	locale=DE,
	output-decimal-marker = {.},
	group-separator = {'},
	per-mode = symbol,
}

%Landscape Geometry
\usepackage{pdflscape}



% ------------------------------------------------------------------------------

% ------------------------------------------------------------------------------
% Glossary & Acronyms
%
% ------------------------------------------------------------------------------
\usepackage[toc,numberedsection=false,shortcuts]{glossaries}
\makeglossaries

%Reset Glossary for each Chapter
\usepackage{etoolbox}
\preto\chapter\glsresetall

% ------------------------------------------------------------------------------
% Code Highlighting
%
% ------------------------------------------------------------------------------
\usepackage[cache]{minted}
\definecolor{bg}{rgb}{0.95,0.95,0.95}
\newminted{csharp}{breaklines=true,linenos,fontsize=\footnotesize,bgcolor=bg}
\newminted{fancy}{breaklines=true,linenos,fontsize=\footnotesize,bgcolor=bg}
\newminted{js}{breaklines=true,linenos,fontsize=\footnotesize,bgcolor=bg}
\newminted{bash}{breaklines=true,linenos,fontsize=\footnotesize,bgcolor=bg}
\newminted{ruby}{breaklines=true,linenos,fontsize=\footnotesize,bgcolor=bg}
\newminted{python}{breaklines=true,linenos,fontsize=\footnotesize,bgcolor=bg}

\renewcommand\listingscaption{Auflistung}



% ------------------------------------------------------------------------------
% BibTeX (Biliography)
% ------------------------------------------------------------------------------
\usepackage[backend=biber, url=true]{biblatex}
\usepackage[babel,german=guillemets]{csquotes}

% ------------------------------------------------------------------------------
% General purpose macros
% ------------------------------------------------------------------------------

% Remove left and right spaces (e.g. macro parameters)
\def\trim#1{\ignorespaces#1\unskip}


% Create horizontal rule command with 1 argument of height
\newcommand{\hr}[1]{\rule{\linewidth}{#1}}

%-------------------------------------------------------------------------------
% Tables
%-------------------------------------------------------------------------------
\usepackage{booktabs}
\usepackage{ltablex}
\usepackage{diagbox}
\usepackage{array}
\usepackage{colortbl}
\usepackage{xcolor}
\usepackage{tabularx}


%-------------------------------------------------------------------------------
% Links
%-------------------------------------------------------------------------------

\usepackage[
	unicode,
	colorlinks,        % Links ohne Umrandungen in zu w�hlender Farbe
	linkcolor=black,   % Farbe interner Verweise
	filecolor=black,   % Farbe externer Verweise
	citecolor=black    % Farbe von Zitaten
]{hyperref}

% ------------------------------------------------------------------------------
% Other styling
% ------------------------------------------------------------------------------
\usepackage[paper=a4paper,
%lmargin=2.2cm, % mittlerer Rand
%rmargin=1.5cm, % äußerer Rand -> ab hier wieder reinnehmen
inner=1.7cm,
outer=1.5cm,
tmargin=1.7cm,
headsep=5mm,
bmargin=2.2cm,
marginparwidth= 0cm, % Breite des Marginpars
marginparsep = 3mm,
includehead, % Kopfzeile in Berechnung einbeziehen
includefoot, % Fusszeile in Berechnung einbeziehen
includemp, % Marginpar in die Berechnung mit einbeziehen
bindingoffset=5mm
]{geometry}
\usepackage{float}

% ------------------------------------------------------------------------------
% User Story / Epic Styling
% ------------------------------------------------------------------------------
\usepackage{mdframed}
\usepackage{framed,color}
\usepackage{tikz,lipsum,lmodern}
\usepackage[most]{tcolorbox}
% Use Jira Colors
\definecolor{epic}{RGB}{247,146,50}
\definecolor{story}{RGB}{103,171,73}

% Chapter without Outputing Chapter Heading but Set Header/Footer -> for Aufgabenstellung

\makeatletter
\newcommand{\unchapter}[1]{%
  \begingroup
  \let\@makechapterhead\@gobble % make \@makechapterhead do nothing
  \chapter{#1}
  \endgroup
}
\makeatother


% ------------------------------------------------------------------------------
% Header / Footer Styling
% ------------------------------------------------------------------------------
\usepackage[automark,headsepline,footsepline]{scrpage2}
\pagestyle{scrheadings}

\renewcommand{\headfont}{\normalfont}
\renewcommand{\pnumfont}{\normalfont}
% \renewcommand*{\chapterheadstartvskip}{\vspace*{0.05cm}}
% Empty in center
\cfoot{}
\chead{}

% Head even
\lehead{\pagemark}
\rehead{\headmark}

% Foot even
\lefoot{\pagemark}
\refoot{}
% Head odd
\lohead{\MakeUppercase{\headmark}}
\rohead{\pagemark}
% Foot odd
\lofoot{}
\rofoot{\pagemark}
